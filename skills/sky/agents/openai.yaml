interface:
  display_name: "Sky"
  short_description: "Answer sky-take-out questions from local materials."
  default_prompt: "Use $sky to answer a sky-take-out learning question from local course materials."

policy:
  allow_implicit_invocation: false
