interface:
  display_name: "Sky"
  short_description: "Search sky-take-out course materials."
  default_prompt: "Use $sky to answer a sky-take-out course question from local materials."

policy:
  allow_implicit_invocation: false
